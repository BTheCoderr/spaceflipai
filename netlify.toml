# Deploy the static marketing site (not the Expo mobile app).
# See marketing/README.md for details.

[build]
  base = "marketing"
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
